# Web Framework
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Groq API (Llama 3.3 70B - Ucretsiz)
groq>=0.4.2

# Opsiyonel: Redis cache (REDIS_URL env varsa kullanilir, yoksa in-memory fallback)
redis>=5.0.0

# HTTP client (market_data icin keep-alive + gzip; yoksa urllib fallback)
requests>=2.31.0

# AI Psikolog dokuman/gorsel ekleri: Spaces upload (S3-compatible) + metin cikarma
boto3>=1.34.0
pypdf>=4.0.0
python-docx>=1.1.0

# MongoDB driver (RAG commodity fetcher backend Atlas'tan okumak icin)
# Yoksa fetch_commodity_local sessizce devre disi kalir.
pymongo>=4.6.0

# Rate limiting (REDIS_URL varsa redis, yoksa memory storage)
flask-limiter>=3.5.0

# Observability (opsiyonel; kurulu degilse no-op fallback ile devre disi kalir)
prometheus-flask-exporter>=0.23.0
sentry-sdk[flask]>=1.40.0

# RAG (opsiyonel; bagimliliklar yoksa rag/ paketi sessizce devre disi kalir).
# CPU sentence-transformers ~120MB indirir (ilk run'da). Server'da kurmak istemiyorsan
# yorum satirina alabilirsin - app.py rag yoksa kendiliginden atlar.
sentence-transformers>=2.7.0
chromadb>=0.5.0
# Linux/Docker icin: chromadb sistem sqlite3 >=3.35 ister; eski distrolarda pysqlite3
# swap ile cozuluyor. Windows'ta sistem sqlite3 yeterli, wheel yok - skip ediyoruz.
pysqlite3-binary>=0.5.0; sys_platform == "linux"

# Opsiyonel - Eski TensorFlow modelleri için
# transformers>=4.36.0
# torch>=2.1.0
# accelerate>=0.25.0
# sentencepiece>=0.1.99

# AI Psikolog goruntulu gorusmede dusuk-guvenilirlikli duygu ipucu (opsiyonel;
# bagimliliklar yoksa /psychology/emotion-hint devre disi kalir, bkz. app.py).
# DeepFace ilk calistirmada onceden egitilmis model agirliklarini indirir
# (~100-300MB), CPU'da calisir ama GPU olmadan turn basina 0.5-2s ekleyebilir.
deepface>=0.0.93
opencv-python-headless>=4.9.0
# TensorFlow 2.16+ varsayilan olarak Keras 3'e gecti, ama DeepFace hala eski
# tf.keras API'sini bekliyor - tf-keras uyumluluk paketi olmadan DeepFace
# import hatasi verip devre disi kaliyor (uygulama cokmez, sadece yuz analizi
# calismaz - bkz. app.py'deki try/except).
tf-keras>=2.16.0
